{
  "name": "weatherflow-udp-server",
  "version": "1.0.0",
  "description": "Grabs Weatherflow UDP broadcasts and stores the data into databases. Currently supports MongoDB and InfluxDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brandon Wilson",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0",
    "influx": "^5.6.0",
    "mongodb": "^3.5.9"
  }
}
